import React from 'react';
import Hero from '../components/Hero';

export const Home = () => {
    return (
        <section className="home">
            <Hero />
        </section>
    );
};

export default Home;